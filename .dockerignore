# Git files
.git
.gitignore

# Node modules (we'll install them inside the container)
node_modules

# Next.js build output (generated inside the container)
.next

# Environment files (should be passed at runtime, not baked in)
.env*
!.env.example # You might want to keep an example file

# Docker files
Dockerfile
docker-compose.yml
.dockerignore

# OS files
.DS_Store
Thumbs.db

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.vscode